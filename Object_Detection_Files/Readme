you have to run this lines in terminal of VS code

"pip install opencv-python"
"pip install"
"pip install flask"

and also install python extension in VS code and Live server.

after that run the Test.py without Debugging.
